package com.zenika.liquid.democracy.api.util;

import java.util.Map;

public class AuthenticationUtil {

	public static String getUserIdentifiant(Map authentication) {
		// if (authentication != null) {
		// Map<String, Object> details = (Map<String, Object>)
		// authentication.getUserAuthentication().getDetails();
		//
		// JSONArray emailArray = new
		// JSONArray(details.get("emails").toString());
		//
		// Map<String, Object> zenikaEmail = (Map<String, Object>)
		// emailArray.get(0);
		//
		// return zenikaEmail.get("value").toString();
		// } else {
		// return "";
		// }
		return "";
	}

}
